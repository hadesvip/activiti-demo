<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

  <!--加载配置文件-->
  <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <property name="locations">
      <array>
        <value>classpath:app.properties</value>
      </array>
    </property>
  </bean>

  <!--datasouce 配置-->
  <bean id="datasource" class="com.alibaba.druid.pool.DruidDataSource">
    <property name="driverClassName" value="${activiti.jdbcDriver}"/>
    <property name="url" value="${activiti.jdbc.url}"/>
    <property name="username" value="${activiti.jdbcUsername}"/>
    <property name="password" value="${activiti.jdbcPassword}"/>
  </bean>

  <!--流程引擎配置-->
  <!--StandaloneProcessEngineConfiguration这个类被用于构建ProcessEngine，
  有多个可用类被用于定义processEngineConfiguration,代表了不同的环境和设置默认配置。-->
  <bean id="processEngineConfig"
    class="org.activiti.engine.impl.cfg.StandaloneProcessEngineConfiguration">
    <property name="dataSource" ref="datasource"/>
    <!--此属性可以不设置，从数据库连接自动检测-->
    <property name="databaseType" value="mysql"/>
    <!--此属性用于数据库脚本更新-->
    <property name="databaseSchemaUpdate" value="true"/>
    <!--作业执行者-->
    <property name="jobExecutorActivate" value="false"/>
    <!--启动一部作业执行者-->
    <property name="asyncExecutorEnabled" value="true"/>
    <!--指示Activiti引擎启动时启动异步执行器线程池-->
    <property name="asyncExecutorActivate" value="true"/>

    <!--事件监听-->
    <property name="eventListeners">
      <list>
        <bean class="com.activiti.listener.MyEventListener"></bean>
      </list>
    </property>

    <property name="typedEventListeners">
      <map>
        <entry key="JOB_EXECUTION_SUCCESS,JOB_EXECUTION_FAILURE">
          <list>
            <bean class="com.activiti.listener.MyEventListener"/>
          </list>
        </entry>
      </map>
    </property>
  </bean>

  <bean id="threadFactory" class="org.springframework.jndi.JndiObjectFactoryBean">
    <property name="jndiName" value="java:jboss/ee/concurrency/factory/default"/>
  </bean>

  <!--<bean id="customJobExecutor" class="org.activiti.engine.impl.jobexecutor.ManagedJobExecutor">-->
  <!--<property name="threadFactory" ref="threadFactory"/>-->
  <!--</bean>-->

  <!--异步执行者-->
  <bean id="asyncJobExecutor"
    class="org.activiti.engine.impl.asyncexecutor.ManagedAsyncJobExecutor">
    <property name="threadFactory" ref="threadFactory"/>
  </bean>


</beans>